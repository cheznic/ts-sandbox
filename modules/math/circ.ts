export const PI = Math.PI;

export function circ_area(radius: number): number {
   return radius * radius * PI;
}

export function circumference(diameter: number): number {
   return diameter * PI;
}
